<!DOCTYPE html>
<html lang="<?= $lang->getLocale() ?>">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= $lang->t('app_name') ?></title>
    <link rel="icon" type="image/svg+xml" href="<?= $baseUrl ?>/favicon.svg">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.1/dist/chart.umd.min.js"></script>
    <link href="<?= $baseUrl ?>/assets/css/style.css" rel="stylesheet">
    <style>
        body {
            user-select: none;
            -webkit-user-select: none;
        }
    </style>
</head>
<body>
    <div id="sidebarOverlay"></div>

    <nav class="sidebar" id="sidebar">
        <a href="<?= $baseUrl ?>/" class="brand">
            <i class="bi bi-star-fill"></i>
            <span><?= $lang->t('app_name') ?></span>
        </a>
        <div class="nav flex-column">
            <a class="nav-link" href="<?= $baseUrl ?>/">
                <i class="bi bi-house-heart"></i>
                <span><?= $lang->t('dashboard') ?></span>
            </a>
            <a class="nav-link" href="<?= $baseUrl ?>/children">
                <i class="bi bi-people-fill"></i>
                <span><?= $lang->t('children') ?></span>
            </a>
            <a class="nav-link" href="<?= $baseUrl ?>/points/history">
                <i class="bi bi-clock-history"></i>
                <span><?= $lang->t('transaction_history') ?></span>
            </a>
            <a class="nav-link" href="<?= $baseUrl ?>/categories">
                <i class="bi bi-bookmark-star-fill"></i>
                <span><?= $lang->t('categories') ?></span>
            </a>
            <a class="nav-link" href="<?= $baseUrl ?>/rewards">
                <i class="bi bi-gift-fill"></i>
                <span><?= $lang->t('rewards') ?></span>
            </a>
            <a class="nav-link" href="<?= $baseUrl ?>/reports">
                <i class="bi bi-graph-up-arrow"></i>
                <span><?= $lang->t('reports') ?></span>
            </a>
            <a class="nav-link" href="<?= $baseUrl ?>/activity">
                <i class="bi bi-activity"></i>
                <span><?= $lang->t('activity_log') ?></span>
            </a>
            <a class="nav-link" href="<?= $baseUrl ?>/settings">
                <i class="bi bi-gear-fill"></i>
                <span><?= $lang->t('settings') ?></span>
            </a>
        </div>
    </nav>

    <div class="main-content">
        <div class="top-navbar">
            <button class="btn d-lg-none" id="sidebarToggle">
                <i class="bi bi-list"></i>
            </button>
            <div></div>
            <div class="d-flex align-items-center gap-3">
                <div class="btn-group btn-group-sm">
                    <a href="<?= $baseUrl ?>/language/en" class="btn btn-<?= $lang->getLocale() === 'en' ? 'primary' : 'outline-primary' ?>">EN</a>
                    <a href="<?= $baseUrl ?>/language/ka" class="btn btn-<?= $lang->getLocale() === 'ka' ? 'primary' : 'outline-primary' ?>">KA</a>
                </div>
                <div class="dropdown">
                    <button class="btn dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="bi bi-person-circle"></i>
                        <span class="d-none d-sm-inline"><?= htmlspecialchars($_SESSION['user']['display_name'] ?? 'Admin') ?></span>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end">
                        <li>
                            <a class="dropdown-item" href="<?= $baseUrl ?>/logout">
                                <i class="bi bi-box-arrow-right me-2"></i><?= $lang->t('logout') ?>
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="content-area">
            <?php include __DIR__ . '/../partials/flash.phtml'; ?>
            <?= $content ?>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="<?= $baseUrl ?>/assets/js/app.js"></script>
    <script src="<?= $baseUrl ?>/assets/js/charts.js"></script>
</body>
</html>
